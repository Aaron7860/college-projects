#include <stdio.h>
int main(){
    short a = -32768;
    printf("%d", a);

    return 0;
}

// short is another data type which stores integer but in between -32768 to +32767 same goes for long and long long